package com.aiassistant.scheduler.executor.handlers;

import utils.ActionCallback;
import utils.ActionHandler;

/**
 * Interface for handlers that execute actions
 * 
 * This interface extends the main ActionHandler interface to standardize
 * handler implementations across the application.
 */
public interface ActionHandler extends com.aiassistant.scheduler.executor.ActionHandler {
    // No additional methods needed - all methods are inherited
    // from the parent ActionHandler interface
}