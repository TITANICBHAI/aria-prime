<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- AppTheme: used by the MainActivity migration stub.
         Extends Theme.AppCompat.Light.NoActionBar (always available via
         androidx.appcompat:appcompat which is in the non-RN dependency block). -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Theme.ARIAAgent: used by ComposeMainActivity (Jetpack Compose UI).
         Extends Theme.MaterialComponents.DayNight.NoActionBar so Material3
         components render without a toolbar.
         Phase 8 note: this is the only theme that survives the RN cleanup. -->
    <style name="Theme.ARIAAgent" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
    </style>

</resources>
